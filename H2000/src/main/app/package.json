{
  "name": "vcenter_plugin",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start:p": "npm install && bower install && cross-env NODE_ENV=production && gulp && ant -f ../../../build-war.xml",
    "start:d": "npm install && bower install && cross-env NODE_ENV=development && gulp && ant -f ../../../build-war.xml",
    "build:p": "cross-env NODE_ENV=production gulp && ant -f ../../../build-war.xml",
    "build:d": "cross-env NODE_ENV=development gulp && ant -f ../../../build-war.xml"
  },
  "dependencies": {},
  "devDependencies": {
    "cross-env": "^3.1.4",
    "del": "^2.2.0",
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.0",
    "gulp-inject": "^4.2.0",
    "gulp-less": "^3.0.5",
    "gulp-minify-css": "^1.2.4",
    "gulp-minify-html": "^1.0.6",
    "gulp-rename": "^1.2.2",
    "gulp-sequence": "^0.4.5",
    "gulp-uglify": "~1.3.0",
    "main-bower-files": "^2.11.1"
  }
}
